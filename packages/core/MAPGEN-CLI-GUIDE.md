# MapGen CLI 工具使用指南

## 📖 简介

MapGen CLI 工具是一个交互式命令行工具，用于加载和调试 Cataclysm-DDA 的地图生成（mapgen）数据。

## 🚀 快速开始

### 运行工具

```bash
cd packages/core
pnpm mapgen-tool
```

## 🎮 功能说明

### 主菜单

启动后，你会看到主菜单：

```
═══════════════════════════════════════════════════════════
主菜单:
═══════════════════════════════════════════════════════════
1. 列出所有 mapgen
2. 搜索 mapgen
3. 随机选择一个 mapgen
4. 按索引选择 mapgen
0. 退出
═══════════════════════════════════════════════════════════
```

### 1. 列出所有 mapgen

选择 `1` 可以浏览所有可用的 mapgen 数据。

**分页浏览**：
- `n` - 下一页
- `p` - 上一页
- `编号` - 查看指定编号的 mapgen 详情
- `0` - 返回主菜单

**示例输出**：
```
第 1/5 页:

[0] house_standard_2
      ID: house_standard_2
      文件: house_building.json

[1] house_base
      ID: house_base
      文件: house_base.json

...
```

### 2. 搜索 mapgen

选择 `2` 可以按关键词搜索 mapgen。

**支持搜索的字段**：
- 名称 (name)
- ID (id)

**示例**：
```
请输入搜索关键词: house

找到 15 个匹配结果:

[0] house_standard_2 (ID: house_standard_2)
[1] house_base (ID: house_base)
...
```

### 3. 随机选择

选择 `3` 可以随机选择一个 mapgen 并生成地图。

**适用场景**：
- 快速测试
- 探索不同的 mapgen
- 发现意外的地图生成结果

### 4. 按索引选择

选择 `4` 可以直接输入索引号来选择特定的 mapgen。

**示例**：
```
请输入索引 [0-234]: 42
```

## 🗺️ MapGen 详情页面

选择一个 mapgen 后，会进入详情页面：

```
╔═══════════════════════════════════════════════════════════╗
║ Mapgen 详情                                                ║
╚═══════════════════════════════════════════════════════════╝

名称: house_standard_2
ID: house_standard_2
文件: /path/to/house.json

═══════════════════════════════════════════════════════════
原始数据:
═══════════════════════════════════════════════════════════
{... JSON 数据 ...}

═══════════════════════════════════════════════════════════
操作:
═══════════════════════════════════════════════════════════
1. 生成地图
2. 生成多个样本
0. 返回
═══════════════════════════════════════════════════════════
```

### 生成地图

选择 `1` 可以生成并显示地图。

**输出示例**：
```
🔄 正在生成地图...

✅ 解析成功
   OM Terrain: house_brick_wood
   尺寸: 12x12
✅ 生成成功

═══════════════════════════════════════════════════════════
生成的地图:
═══════════════════════════════════════════════════════════

############
#..........#
#..+........#
#..........#
#...@......#
#..........#
#..........#
#..........#
############

═══════════════════════════════════════════════════════════
操作: [0]返回 [r]重新生成 [s]保存
═══════════════════════════════════════════════════════════
```

**地形符号说明**：
- `#` - 墙壁
- `.` - 地板
- `+` - 门
- `~` - 水
- `"` - 草
- `:` - 路
- `>` - 下楼梯
- `<` - 上楼梯
- `?` - 未知地形

### 生成多个样本

选择 `2` 可以生成多个样本进行对比。

**示例**：
```
请输入生成数量 [1-10]: 3

🔄 正在生成 3 个样本...

─────────────────────────────────────────────────────────
样本 1/3
─────────────────────────────────────────────────────────

############
#..........#
#..+........#
...

─────────────────────────────────────────────────────────
样本 2/3
─────────────────────────────────────────────────────────

############
#..........#
#..........#
...
```

**用途**：
- 测试随机性
- 对比不同种子生成的地图
- 调试生成逻辑

## 🔧 高级功能

### 调试流程

1. **发现错误**
   - 生成地图时注意观察错误信息
   - 检查是否有缺失的地形定义
   - 查看是否有意外的瓦片

2. **查看原始数据**
   - 在详情页面可以看到完整的 JSON 数据
   - 检查 omTerrain、rows、terrain 等字段
   - 确认数据格式是否正确

3. **对比不同样本**
   - 使用"生成多个样本"功能
   - 对比随机性是否正常
   - 检查是否有固定的模式

### 常见问题

#### 1. 找不到 mapgen 数据

**症状**：
```
❌ 没有找到任何 mapgen 数据
   路径: /path/to/mapgen
```

**解决**：
- 检查 Cataclysm-DDA 数据路径是否正确
- 确认 `data/json/mapgen` 目录存在
- 确认目录中有 `.json` 文件

#### 2. 生成失败

**症状**：
```
❌ 生成失败: Error: ...
```

**可能原因**：
- 地形定义缺失
- 家具定义缺失
- mapgen 数据格式错误

**解决**：
- 查看完整错误信息
- 检查是否加载了必要的数据（terrain.json, furniture.json）
- 确认 mapgen JSON 格式正确

#### 3. 地图全是 `?`

**症状**：
```
?????????????
?????????????
?????????????
```

**可能原因**：
- 地形 ID 无法解析
- 缺少地形数据

**解决**：
- 检查 terrain loader 是否正确加载
- 确认地形定义文件存在
- 查看解析后的 mapgen 数据中的 terrain 字段

## 📁 数据目录结构

工具默认从以下路径加载数据：
```
/Users/tanghao/workspace/game/Cataclysm-DDA/data/json/
```

**数据文件分布**：
```
data/json/
├── furniture_and_terrain/
│   ├── terrain-*.json (25 个文件，包含地形定义)
│   └── furniture-*.json (30 个文件，包含家具定义)
├── traps.json (陷阱定义)
└── mapgen/
    └── *.json (268 个 mapgen 定义文件)
```

## 🛠️ 自定义数据路径

可以通过修改构造函数参数来自定义数据路径：

```typescript
const cli = new MapGenCLI('/path/to/custom/mapgen');
await cli.run();
```

## 💡 使用技巧

### 1. 快速测试特定 mapgen

如果知道 mapgen 的索引，可以直接使用"按索引选择"功能：

```
主菜单 > 4 > 输入索引 > 生成地图
```

### 2. 批量测试

使用"生成多个样本"功能可以快速验证 mapgen 的随机性：

```
详情页 > 2 > 输入数量 (如 5) > 查看结果
```

### 3. 查找特定类型的 mapgen

使用搜索功能按关键词查找：

```
主菜单 > 2 > 输入 "house" > 浏览结果
```

常见关键词：
- `house` - 房屋
- `road` - 道路
- `forest` - 森林
- `underground` - 地下
- `lab` - 实验室

### 4. 对比相似 mapgen

搜索关键词后，选择不同的 mapgen 进行对比：

```
主菜单 > 2 > "house" > 记录索引号
主菜单 > 4 > 输入索引 > 生成
主菜单 > 4 > 输入另一个索引 > 生成
对比两次的结果
```

## 📊 统计信息

工具启动时会显示：

```
📂 正在扫描 mapgen 数据...
✅ 找到 123 个 JSON 文件
✅ 加载了 456 个 mapgen 定义
```

这表示：
- 扫描到的 JSON 文件数量
- 成功解析的 mapgen 对象数量

## 🎯 调试工作流

推荐的调试工作流：

1. **加载并查看**
   ```
   运行工具 > 列出 mapgen > 选择一个 > 查看详情
   ```

2. **生成测试**
   ```
   详情页 > 生成地图 > 查看结果
   ```

3. **多次测试**
   ```
   详情页 > 生成多个样本 (3-5个) > 对比结果
   ```

4. **对比不同 mapgen**
   ```
   搜索关键词 > 选择不同结果 > 分别生成
   ```

5. **记录问题**
   - 记录出现错误的 mapgen ID
   - 截图或复制错误信息
   - 记录地形符号分布

## 🚀 下一步

- 使用此工具调试特定的 mapgen
- 验证地形生成逻辑
- 测试随机性和变化
- 发现和修复生成 bug

## 📝 示例会话

```
$ pnpm mapgen-tool

╔═══════════════════════════════════════════════════════════╗
║       Cataclysm-DDA MapGen 调试工具                        ║
╚═══════════════════════════════════════════════════════════╝

📂 正在扫描 mapgen 数据...
✅ 找到 45 个 JSON 文件
✅ 加载了 123 个 mapgen 定义

═══════════════════════════════════════════════════════════
主菜单:
═══════════════════════════════════════════════════════════
1. 列出所有 mapgen
2. 搜索 mapgen
3. 随机选择一个 mapgen
4. 按索引选择 mapgen
0. 退出
═══════════════════════════════════════════════════════════

请选择操作 [0-4]: 2

═══════════════════════════════════════════════════════════
搜索 Mapgen
═══════════════════════════════════════════════════════════

请输入搜索关键词: house

找到 15 个匹配结果:

[0] house_standard_2 (ID: house_standard_2)
[1] house_base (ID: house_base)
...

请输入编号查看详情 [0-14] (或按 0 返回): 0

🔄 正在生成地图...

✅ 解析成功
   OM Terrain: house_brick_wood
   尺寸: 12x12
✅ 生成成功

═══════════════════════════════════════════════════════════
生成的地图:
═══════════════════════════════════════════════════════════

############
#..........#
#..+........#
#..........#
#..........#
#..........#
#..........#
#..........#
############

═══════════════════════════════════════════════════════════
操作: [0]返回 [r]重新生成 [s]保存
═══════════════════════════════════════════════════════════

请选择: 0
```

---

**最后更新**: 2025-01-10
**版本**: 1.0.0
**状态**: ✅ 可用
